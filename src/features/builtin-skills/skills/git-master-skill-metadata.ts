export const GIT_MASTER_SKILL_NAME = "git-master"

export const GIT_MASTER_SKILL_DESCRIPTION =
  "Use for COMPLEX git operations only: multi-file atomic commits requiring careful splitting, rebase/squash, history search (blame, bisect, log -S). DO NOT load for trivial operations (single file add+commit, checking status, simple one-liner commits) -- just run the git commands directly. Triggers: 'atomic commits', 'rebase', 'squash', 'who wrote', 'when was X added', 'find the commit that', 'split commits', 'cleanup history'."
